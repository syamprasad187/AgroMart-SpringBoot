# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: SDPProject
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sdpproject
    username: root
    password: Mahi12syam$
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    view:
      prefix: /
      suffix: .jsp
server:
  port: 2532
razorpay:
  key:
    id: rzp_test_M8euIoyO42vv2E
    secret: q6Qhgpi8gDR8ng5CuWHieE7G
